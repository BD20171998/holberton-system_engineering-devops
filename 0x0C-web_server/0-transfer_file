#!/usr/bin/env bash
#Bash script that transfers a file from a client to a server

if [ "$#" -lt 3 ];
then
    echo "Usage: 0-transfer_file PATH_TO_FILE IP USERNAME PATH_TO_SSH_KEY"
else
    ssh -o StrictHostKeyChecking=no "$3@$2" -i ~/"$4"
    scp "$1" "$3@$2" "$3@$2":~/
fi
